.container
  %h3 Regular Expression
  = "/#{@pattern.regex}/"
  %h3 Codes
  - @pattern.codes.order(:number).each do |c|
    %ul #{c.full_name}

  %br
  = link_to "edit this pattern", [:edit, @pattern]
  %br
  = link_to "add new pattern", new_pattern_path
